export const apiRoutes = {
    refreshToken: 'users/refresh',
    login: 'users/login',
    logout: 'users/logout',
    signUp: 'users/signup',
}

export const pageRoutes = {
    home: '/',
    auth: '/auth',
}
